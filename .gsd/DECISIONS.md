# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001/Discuss | platform | Primary deployment platform | AWS Bedrock AgentCore | Zscaler officially supports this path with Marketplace image and CloudFormation templates. User wants officially supported methods. | No — unless Zscaler adds new official platforms |
| D002 | M001/Discuss | ux | Deployment execution mode | Perform deployment from terminal (not just generate templates) | User wants CLI to actually deploy, not just prepare. | No — core product definition |
| D003 | M001/Discuss | ux | User experience | One command + interactive prompts | Removes friction, guides operator through required inputs. | No — core UX pattern |
| D004 | M001/Discuss | security | Credential storage | AWS Secrets Manager only (not direct env vars) | User wants strict/safest path. Secrets Manager provides KMS encryption, CloudTrail audit, rotation support. | Revisable — could add direct env vars later for testing if user requests |
| D005 | M001/Discuss | validation | Preflight strictness | Strict preflight that stops early with exact fixes | User explicitly said "strict" and "none" for assumed prerequisites. Fail fast with actionable errors. | No — core reliability pattern |
| D006 | M001/Discuss | product | Product scope | Network/security MCP deployer (not generic MCP framework) | User explicitly said "for network/security mcp", not any MCP. Keeps scope focused. | Revisable — if product direction changes |
| D007 | M001/Discuss | verification | Success criteria | Runtime up + connection steps (not just CREATE_COMPLETE) | User wants verification that it actually works, not just that AWS accepted the request. | No — defines "done" for deploy |
| D008 | M001/S01/T01 | cli | CLI framework | Typer with Rich for CLI structure | Typer provides modern, type-annotated command structure with automatic help generation. Rich provides enhanced terminal output formatting. Well-maintained libraries with good documentation. | Revisable — could switch if requirements change significantly |
| D009 | M001/S01/T02 | validation | AWS session validation approach | Comprehensive credential chain validation with region support checking | Validates credentials through all standard AWS credential providers (env vars, credentials file, profiles) and checks region support for Bedrock. Provides detailed error messages for each failure mode. | Revisable — if AWS authentication requirements change |
| D010 | M001/S01/T03 | validation | IAM permission validation approach | Practical validation by attempting service calls | Rather than complex IAM policy simulation, use actual service calls to validate permissions. This provides more accurate results and is easier to implement and maintain. | Revisable — could add more sophisticated simulation later if needed |
| D011 | M001/S01/T03 | validation | Required AWS permissions scope | Focus on Bedrock, SecretsManager, and STS services | These are the core services required for Zscaler MCP deployment. Keeping scope focused reduces complexity and improves reliability. | Revisable — if deployment requirements change |
| D012 | M001/S01/T03 | cli | IAM validation integration | Optional IAM validation with skip flag | IAM validation adds complexity and potential delays, so make it optional with --skip-iam flag while keeping it enabled by default for strict validation. | Revisable — could change default behavior based on user feedback |
| D013 | M001/S01/T04 | validation | Zscaler credential validation approach | Format validation with connectivity and authentication testing | Validate Zscaler credential format (email, 32-char hex API key), test connectivity to cloud endpoints, and verify authentication with actual API calls. This provides comprehensive validation without complex simulation. | Revisable — if Zscaler API requirements change |
| D014 | M001/S01/T05 | error_handling | Error messaging system | Structured error hierarchy with context-rich messages and user guidance | Implement comprehensive error messaging system with custom exception types, predefined error messages, and detailed user guidance. This provides actionable error messages with specific remediation steps and enhances the overall user experience. | Revisable — could evolve based on user feedback and additional error scenarios |
| D015 | M001/S02/T01 | architecture | AWS module organization | Create `aws/` package for AWS resource modules | Separates AWS resource operations (secrets, IAM) from validators for cleaner code organization. Allows both `validators/iam.py` and `aws/iam_bootstrap.py` to coexist without confusion. | Revisable — if codebase structure changes |
| D016 | M001/S02/T01 | architecture | Shared dataclasses | Create `models.py` for result dataclasses | Centralizes `SecretResult`, `RoleResult`, `BootstrapResult` dataclasses used across S02 modules. Provides clean API contracts between components. | Revisable — could use separate files per module if models grow large |
| D017 | M001/S02/T02 | naming | IAM bootstrap module naming | Use `iam_bootstrap.py` to avoid conflict with `validators/iam.py` | Existing `validators/iam.py` handles permission validation; new `aws/iam_bootstrap.py` handles role creation. Different concerns require separate modules. Clear naming prevents import confusion. | No — naming follows established project conventions |
| D018 | M001/S02/T01 | architecture | Lazy boto3 initialization | Create clients on first access, not in __init__ | Enables test dependency injection by allowing mocked Session to be set after initialization. Pattern established in SecretsManager and reused in IAMBootstrap. | No — core testability pattern |
| D019 | M001/S02/T01 | security | Secret logging redaction | Never log secret values, only ARNs and names | Security requirement — secret values (passwords, API keys) must never appear in logs or error messages. All Result dataclasses and error messages only include ARNs. | No — security invariant |
| D020 | M001/S02/T02 | reliability | IAM propagation wait | Exponential backoff with 1s + 2s + 4s retries, capped at 15s total | AWS IAM has eventual consistency. Newly created roles may not be immediately assumable. Exponential backoff provides faster feedback than fixed 15s wait while still hitting the target. | Revisable — timing may need adjustment based on real AWS behavior |
| D021 | M001/S02/T02 | error_handling | Resource existence validation | Check compatibility when resources already exist | For secrets: return existing ARN. For IAM roles: validate trust policy has bedrock.amazonaws.com principal. Raise specific error (TrustPolicyMismatchError) if incompatible. | No — idempotency requirement |
| D022 | M001/S02/T03 | reliability | Transactional rollback | Delete resources in reverse order on partial failure | If role creation fails after secret was created, delete the secret to leave AWS in clean state. Reverse order (role first, then secret) handles dependencies correctly. | No — reliability requirement |
| D023 | M001/S02/T04 | ux | Interactive vs non-interactive modes | Prompt for missing values by default, --non-interactive to fail fast | Default interactive mode guides users through required inputs. --non-interactive flag enables CI/CD automation with clear error messages. | Revisable — could adjust default based on usage patterns |
| D024 | M001/S03/T01 | architecture | Runtime rollback scope | Rollback only deletes runtime, keeps bootstrap resources | Per R008 deferral and operational wisdom — persistent infrastructure (secret, role) should not be destroyed on transient runtime failures. Keeps resources for troubleshooting and reuse. | Revisable — could add full cleanup flag later if needed |
| D025 | M001/S03/T01 | security | Secret name extraction from ARN | Strip AWS 6-character suffix from secret ARN for env var | AWS adds random 6-char suffix to secret names (e.g., "zscaler-creds-AbCdEf"). Container expects just the base name. Implemented via regex extraction. | No — AWS behavior, must match for credential retrieval to work |
| D026 | M001/S03/T02 | reliability | Status polling strategy | Exponential backoff 5s to 30s, 10-minute default timeout | Prevents API throttling while providing timely updates. Factor of 1.5 gives good balance. 10 minutes covers typical Bedrock runtime creation time (2-5 min) with headroom. | Revisable — timing may need adjustment based on real AWS behavior |
| D027 | M001/S03/T03 | architecture | Phase-based deployment tracking | DeployResult.phase field tracks exact failure location | Phase values: bootstrap, runtime_create, polling, completed. Enables precise diagnostics — operator knows exactly which step failed without reading logs. | No — core observability pattern |
| D028 | M001/S03/T03 | error_handling | Orchestrator error code prefixes | S03-001-* (creation), S03-002-* (polling), S03-003-* (orchestration) | Three distinct error code prefixes enable rapid categorization. S03-001 = AWS API issues, S03-002 = polling/timeout issues, S03-003 = coordination issues. | Revisable — could add sub-codes if needed |